# 新Index モバイルQA・未保存作業の引き継ぎ

## 対象と保存前のGitHub確認

- リポジトリ: `100things-project/poi-days`
- main: `4d204a18d2e5afc3514a55b45d164268edbbb12c`
- feat/mobile-media-index: `7f2fa8611c2cfb8c7f61ab4a44a34d4da370126d`
- feat/daily-site-rankings: `aef331df9b237fe831d4225f5a10e88f99974564`
- GitHubのブランチAPIとローカルの同一commitのファイル・差分を照合。dailyはmobileを引き継ぎ、ランキング・NEWS取得、日替わりおすすめ、3サイト紹介ページ、人気サイトリンクの生成処理を追加していた。
- GitHub上のlive-rankings/live-newsは空。生成済みIndexも新機能を反映していなかった。前回WorkのQA・修正・2026-09-09取得データはローカルにだけ残っていたため、再制作せず監査・再検証して保存。
- feat/weekly-featuresは閲覧・変更・取り込みなし。mainへのマージ・公開なし。

## 修正

- ランキング金額欄、NEWS日付欄の幅を調整し、短いラベルの1文字改行を解消。
- 記事の「招待コード」「紹介リンク」「使い方」等を語句単位に保ち、特集・長い案件名の折り返しを改善。
- 記事一覧リンク、公式ランキングリンク、NEWSリンク、紹介ページのトップリンクに44px以上の操作領域を確保。
- 確認日はランキング全体の注記にまとめ、個別行の細い金額欄には重複表示しない。
- 公式ランキングの案件名から取得元の公式案件URLへ遷移可能にした（同一ホストのHTTPSだけ）。
- 前日以前のランキングにも「前回取得分」を表示。今日の正常データがなければおすすめは既存ガイドのサンプル案内に戻す。
- NEWSに未取得サイトと掲載日の意味を表示。
- 前回の本番相当検証で判明した抽出不良を修正: モッピーの総合ランキングを限定抽出、ワラウで隣接案件・旧還元額を混ぜない、pt単位の欠落を修正、NEWSの文字化け・日付の取り違え・重複・FAQ誤抽出を修正。
- 生成物をdocs/dist両方に反映。サイトマップ検査を新規3ページを含む20件に更新。

## 実ブラウザ確認

Chrome実描画。幅指定iframeの実コンテンツ幅を計測し、スクリーンショットを目視確認。DOM検査だけで判定していない。実機iOS Safariの検証ではない。

| 幅 | 横幅計測 | 画像・文字・余白 | 4タブのクリック | 未取得・前回データ | 画像失敗 |
|---|---|---|---|---|---|
| 375px | clientWidth=scrollWidth=375 | 確認・修正後再確認 | 全4サイト・各5行 | サンプル/確認日/前回取得分を確認 | 代替画像で枠を維持 |
| 390px | clientWidth=scrollWidth=390 | 確認・修正後再確認 | 全4サイト・各5行 | 同上 | 同上 |
| 430px | clientWidth=scrollWidth=430 | 確認・修正後再確認 | 全4サイト・各5行 | 同上 | 同上 |
| 768px | clientWidth=scrollWidth=768 | 確認・修正後再確認 | 全4サイト・各5行 | 同上 | 同上 |

- 通常Indexで画像読み込み不良0件、表示中の操作対象に44px未満の領域なし。
- NEWS、今日のおすすめ（9/10はサンプル案内）、人気サイト欄を確認。
- トップからモッピー/ハピタス/ワラウ/ちょびリッチへ実際にクリックして移動。各ページのトップリンクまたはランキングリンクで戻れることを確認。新規3ページは375pxで全文目視確認。
- 画像失敗は検証専用HTMLで存在しない画像URLを使用し、製品と同一のJS/CSSによる代替表示を4幅で目視確認。検証専用ファイルは配布物・commitに含めない。
- 4サイトすべての403相当の失敗時、前回データがあればその内容を保持し、なければunavailableとなることをオフライン回帰テストでも確認。

## 取得データ（前回の1回取得を保持・再アクセスなし）

2026-09-09 JSTにランキング4URL、NEWS4URLを各1回取得。失敗の再解析は保存済みHTMLを使用。今回9/10のQAでは収集処理のネットワーク実行・Actions実行を追加していない。

| サイト | ランキング | NEWS |
|---|---|---|
| モッピー | 5件、9/9確認、前回取得分 | 0件、HTML内に日付付き一覧なし |
| ハピタス | 0件、一覧が動的読込のためサンプル維持 | 4件、公式掲載日2024-04-01〜2026-06-23 |
| ワラウ | 5件、9/9確認、前回取得分 | 10件、公式掲載日2026-08-05〜09-04 |
| ちょびリッチ | 0件、一覧が動的読込のためサンプル維持 | 2件、公式掲載日2026-08-26・09-01 |

元HTMLの案件名・現在還元表示・公式URLと照合。モッピーの楽天銀行案件は元HTML自体に「12,000P」の案件名と「1,500P」の現在還元欄が併存していた。推測で数値を変更せず、取得元の内容を保持しているため、公開前の再確認事項とする。

## 保全と検査

`check.py`（22ページ・相対リンク・アンカー・招待URL・画像寸法・再生成一致）、`check-seo.py`、`check-preservation.py`、`check-media.mjs`（20ケース）、`check-rankings.py`、`check-news.py`、`check-collector-fallbacks.py`、`check-recommendation.py`がPASS。

dailyの元commitに対し、既存モッピーページ、既存記事・ガイド、交換先、招待URL/コード、GA4、point-sitesの本文データは未変更。

## 残件・公開判断

- ハピタス/ちょびリッチの実ランキング、モッピーNEWSの動的一覧取得は未完了。アクセス制限を回避せず、安全な未取得表示を維持。
- 9/10の新規収集は行っていない。取得日を9/10に書き換えず、9/9の正常データを保持。
- 実機iOS Safariの最終確認、上記の取得未完了項目を許容するかの判断、mainへのマージ承認はユーザー確認待ち。
- PC版制作、mainへのマージ・公開は行わない。
