# POI DAYS 新Index スマホ版

2026-09-09。指示書全12節・埋め込み参考画像を確認し、GitHub main `4d204a18d2e5afc3514a55b45d164268edbbb12c` を基点に実装。

## 完了

- 指定順の10要素（ヘッダー、メイン8セクション、フッター）。375pxを基準にした独立CSS。PC版の本格実装は未着手。
- 新トップは総合メディア。モッピー退避ページの内容・交換先デザイン・紹介情報・共通CSS・app.js・analytics.js・画像は保持。
- moppy.htmlのtitle/canonical/OG URLとホームへのロゴリンクを修正。診断・掲載案件など、移動済み内容への既存リンクをmoppy.htmlへ変更。
- ランキングはタブ切替、矢印キー対応、最大5件、0件時表示。未確認の金額を掲載せずサンプルと明示。
- 新着記事は既存5本。ニュースはサンプル、特集の未整備リンクは準備中。問い合わせは既存の連絡先準備中案内へ。
- JSONから静的HTMLとJSデータを生成。JavaScriptがなくても既存記事・サンプル・ネイティブメニューが利用可能。画像失敗時の代替処理。
- docs/dist同期、19ページの相対リンク・アンカー・紹介URL・画像寸法、再生成一致を確認。
- 全既存ファイルを基点と比較し、上記の許可したメタ情報・リンク以外の差分がないことを機械検証。
- DOMテスト20ケース：0/1/5/12件、切替、長文保持、欠損、不正な画像URL、テキスト安全表示。検索、キーボード、画像フォールバックも検証。

## 未完了と公開判断

**ブラウザー実画面検証は未完了。ユーザー確認前の完了条件はまだ満たしていない。**

プレビュー用Vite設定を追加し、一度起動成功したが、その後接続が切れ、プレビュー停止・ERR_CONNECTION_REFUSEDとなった。復旧手順の試行上限に達したため別のブラウザー手段へ迂回していない。

未確認：375/390/430/768pxの実際の横はみ出し、改行・画像トリミング・タップ領域の目視、件数変化と画像欠損時の実レイアウト、既存ページの実画面回帰。DOM試験はこれらの代替ではない。

mainは変更していない。GitHub Pagesへの公開とPC版への展開はしていない。元のSites版も変更していない。GitHub Actionsや本番収集APIは追加・実行していない。

## 再開手順

1. 最新mainと本ブランチの差分を確認。完成済みを作り直さない。
2. `npm ci`、`python3 build.py`、`python3 check.py`、`python3 scripts/check-seo.py`、`python3 scripts/check-preservation.py`、`node scripts/check-media.mjs`。
3. 画面確認用は `npm run dev`。WorkではSitesの監督付きプレビュー規則に従う。現在の設定はdocsを配信するだけで本番構成には影響しない。
4. 指定4幅で上記の未確認項目を完了させ、スマホ版をユーザーに確認してもらう。その後にPC版へ。

## 編集元

- `content/media-home.html`：総合Indexテンプレート
- `content/media-home.json`：ランキング・新着記事・NEWS・特集データ
- `content/media-home.css` と `content/media-home.js`：新トップ専用
- `scripts/build-media.py`：静的フォールバックとJS用データ生成

`build.py`通常実行で新トップを生成してから既存SEO記事を再生成する。`--legacy-layout-build` は旧構成を上書きするため今回の再開では使わない。

実データ導入時には出典・確認日・対象条件の確認と、静的HTML/JSの表示一致を改めて検証する。現在のデータはサンプル専用で、今日の実ランキングとして利用しない。ポイントサイト名のコンパクト表示は文字で表現しており、公式ロゴの転載ではない。
